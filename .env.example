# Настройки облака. Скопируй в .env и подставь свои значения.
# Взять их можно в Supabase: Project Settings -> API.
# Эти значения попадают в собранный сайт и видны всем — так и задумано:
# anon-ключ без входа ничего не открывает, доступ разрешает RLS (supabase/schema.sql).
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
