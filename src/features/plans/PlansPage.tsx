/**
 * Планирование — заглушка.
 *
 * Тип Plan и коллекция `plans` уже заведены в схеме и в хранилище, поэтому
 * запуск раздела сведётся к написанию экрана: трогать базу и адаптеры не
 * придётся.
 */
export function PlansPage() {
  return (
    <div className="page">
      <div className="page-head">
        <div className="page-title-group">
          <h1>Планы</h1>
          <p className="page-sub">Общий список того, что хотим сделать вместе.</p>
        </div>
      </div>

      <div className="empty">
        <span className="empty-emoji">🗓️</span>
        <div style={{ maxWidth: 460 }}>
          Раздел в работе. Место под него готово: данные уже умеют храниться, осталось
          придумать, как именно мы хотим планировать.
        </div>
      </div>

      <div className="card">
        <h2 style={{ marginBottom: 12 }}>Что здесь появится</h2>
        <ul className="plan-list">
          <li>Идеи «когда-нибудь» и планы с датой — в одном списке.</li>
          <li>Отметка, кого план касается: обоих или кого-то одного.</li>
          <li>Ближайшее — на главной, чтобы не забывалось.</li>
          <li>Выполненный план можно превратить в момент архива одним нажатием.</li>
        </ul>
      </div>
    </div>
  );
}
